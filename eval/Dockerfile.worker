FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev || npm install --omit=dev

COPY eval-server.js ./

RUN addgroup -S evaluser && adduser -S evaluser -G evaluser \
    && chown -R evaluser:evaluser /app

USER evaluser

ENTRYPOINT []
CMD ["node", "eval-server.js"]
