FROM node:22-alpine

WORKDIR /worker

COPY package.json .

RUN npm install --omit=dev \
    && npm cache clean --force

COPY eval-server.js .

RUN addgroup -S sandbox \
    && adduser -S sandbox -G sandbox \
    && chown -R sandbox:sandbox /worker

USER sandbox

CMD ["node", "eval-server.js"]
